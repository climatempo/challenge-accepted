const index = require('../../src/index');

// Only for test pass - TODO: replace with project tests
describe('Build example', () => {
  it('should be defined', () => {
    expect(index).toBeDefined();
  });
});
