import {
  Dimensions,
} from 'react-native'

const { width, height } = Dimensions.get('window')

export default {
  grayLight: '#eeeeee',
  blueLight: '#1565c0',
  
  //Dimensions
  width,
  height,
}
